# Issue Reproduction (Vite v6 + React + TypeScript)

This is a project that serves as reproduction for an issue.

## Testing instructions

Install dependencies:

```bash
npm install
```

Run Storybook init:

```bash
npx storybook@latest init
```
